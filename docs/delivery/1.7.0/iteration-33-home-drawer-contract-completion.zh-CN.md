# 迭代 33：Home 与抽屉契约补齐

> 适用版本契约：[1.7.0 交付](delivery.zh-CN.md)。本契约只定义经授权的交付边界；它不拥有执行状态、证据、提交或结果，本身不授权生产实现、提交、推送、打 tag 或任何发布动作。

## 目标

严格按已验收契约交付 Home 与抽屉上剩余的两个契约面：可关闭的 Home 默认启动器提示卡，以及应用大小块的两条三档渐缩滑杆——并包含一次作者设备校准环节，把渐缩滑杆刻意延后的视觉值定值并回写样式设置面板契约。

## 产品与版本引用

- 产品契约基线：`da22d3d1c4c3e22105a8d6f96256c1e6ffe626d4` — 当前 mainline 头，包含已验收的默认启动器提示契约（`7698498624d1d45efc940eb249a722423b593f68`）与应用大小渐缩滑杆契约。
- 适用产品文档：
  - [Home 行为](../../product/surfaces/home.zh-CN.md)，默认启动器提示一节
  - [Home 呈现](../../product/presentation/home.zh-CN.md)，提示呈现
  - [样式设置面板呈现](../../product/presentation/style-settings-panel.zh-CN.md)，应用大小渐缩滑杆
  - [Settings 行为](../../product/surfaces/settings.zh-CN.md)，与提示共享的默认主屏目的地
  - [隐私与数据处理](../../product/features/privacy.zh-CN.md)，提示披露
- 适用版本契约：`delivery.md`

## 可观察结果

在主设备上，当 Launcher4Max 不是默认启动器时，Home 在基本信息区与收藏主列表之间显示可关闭的提示卡，带三条本地化文案与契约规定的填充、圆角、边界与外边距；其可见性每次进入前台评估一次，不会在本次会话中途插入，应用成为默认后立即消失，Dismiss 后当日剩余时间被抑制，整个编辑模式期间隐藏，点击路由到与 Settings 默认主屏入口相同的系统目的地。Home 主列表应用名、编辑 dock 指令文本、抽屉应用名与搜索框提示文本使用与 Home 基本信息文本相同参数的已接受逐字形阴影，且其他文字处理不变。应用大小块呈现两条带校准视觉值的三档渐缩滑杆，档位吸附于 Large、Medium、Small，轨道在 thumb 处双色分割，每条滑杆具备无障碍标签、档位名与 range actions。

## 纳入工作

- Home 默认启动器提示：有界区域放置、每次进入前台一次的可见性评估、成为默认后立即移除、无提醒次数上限、Dismiss 后当日抑制、关闭日期作为备份文件之外的持久呈现状态存储、编辑模式隐藏、单次布局变化吸收移除与恢复、与 Settings 默认主屏入口共享的 `Settings.ACTION_HOME_SETTINGS` 路由、独立关闭目标旁的非交互箭头提示，以及契约规定的 `72dp` 最小高度、`darkSurfaceBaseColor` 填充、`12dp` 圆角、共享 `12%` 边界、`8dp` 外边距与不透明表面文本处理。
- 应用大小块的两条三档渐缩滑杆作为一个自定义控件：右三角轨道轮廓自左向右递减、吸附档位于左端/中点/右端、仅以位置表达档位的竖条形 thumb 且无文字指示器、thumb 及其左侧轨道用 `primaryTextColor` 其余用 `secondaryTextColor`、`16dp` 间距与剩余行宽均分，以及每条滑杆的本地化标签、当前档位名与每次移动一档的标准本地化 range actions。
- 校准后的滑杆交付值（两端高度、bar 高度、宽度与圆角处理）在作者设备上验收后回写样式设置面板契约。
- 按宿主契约的滑杆禁用行为：被禁用的滑杆报告不可用且不改变档位。
- 提示的三条文案及所有新增或变更的用户可见文案的英文与简体中文字符串，名称完全对齐。
- 聚焦测试源：覆盖提示可见性与抑制状态、Home 与抽屉应用名称、编辑坞提示文案与抽屉搜索框占位提示上的逐字形阴影处理，以及滑杆的档位映射、档位选择、颜色与无障碍语义。

## 排除工作

- 快捷操作、屏幕锁定与无障碍服务迁移，属于迭代 32。
- 发行边界变更与全部发布操作事项，属于迭代 34。
- 任何横屏行为或方向变更。
- 应用大小块之外的滑杆应用；本迭代中其他表面不采用渐缩滑杆。
- 应用大小档位模型本身的任何变更——三档、默认值、持久化与备份内容保持 1.6.0 交付原样。

## 技术变更面

- Home 布局：基本信息区与收藏主列表之间的有界提示区域、其可见性状态与编辑模式隐藏。
- 呈现状态：备份文件之外的持久当日关闭日期。
- 样式设置面板：替换当前应用大小控件的自定义渐缩滑杆控件。
- 隐私资源：若契约的隐私措辞需要资源更新，则同步提示相关披露文案。
- 样式设置面板契约：经授权修订路径的校准回写。
- 测试：上述行为的聚焦本地与仪器测试源。

## 依赖与顺序

本迭代在行为与代码面上独立于迭代 32，仅共享资源重叠；按版本的串行默认排在迭代 32 之后。生产实现开始前，实现线必须包含记录的基线。校准环节需要作者设备参与，且先于滑杆工作的最终验收。

## 迁移与兼容影响

- 关闭日期是备份文件之外的新呈现状态：全新安装或恢复的备份以无抑制状态开始，符合契约。
- 不需要设置存储 schema 变更；应用大小档位按 1.6.0 交付原样持久化。

## 安全、隐私、权限与许可影响

- 不引入新权限、网络、账户或外部服务。
- 提示的可见性规则与关闭状态属于隐私契约披露的呈现状态；若提示披露需要资源更新，隐私措辞在同一次变更中对齐。
- 滑杆是本地呈现行为。

## 风险与未决决策

- 渐缩滑杆的端部高度、bar 高度、宽度与圆角处理有同样的校准依赖。
- 被搁置的抽屉自动化测试簇核心正是滑杆拖拽注入；新自定义滑杆的自动化覆盖可能撞上同一限制，因此交互证据由设备验收承担，自动化测试聚焦映射、颜色与无障碍语义。
- 提示绝不能在无提示的进入前台之后的同一会话内出现，包括同一自然日内的进程死亡后，这要求严格区分持久关闭状态与会话级评估。
- 在 Home 与抽屉应用名称、编辑坞提示文案与抽屉搜索框占位提示上保持已接受的逐字形阴影，必须保持此前已修复的收藏织带宽度与文本裁剪行为。

## 验收标准

- 提示恰在应用不是默认启动器时出现，几何与文本处理符合契约，三条文案双语存在。
- 提示可见性每次进入前台评估一次，不在同一会话中补插，成为默认后立即移除。
- Dismiss 使提示在当日剩余时间被抑制，关闭日期跨重启持久，成为默认不记录关闭。
- 提示路由到与 Settings 默认主屏入口相同的系统目的地，箭头提示非交互，提示在整个编辑模式期间隐藏且单次布局变化吸收移除与恢复。
- Home 与抽屉应用名称、编辑坞提示文案与抽屉搜索框占位提示渲染与 Home 基本信息文本同参数的已接受逐字形阴影，且其他文字处理不变。
- 应用大小块呈现带校准值的两条渐缩滑杆；每条滑杆在三档吸附、thumb 处分割轨道颜色、暴露本地化标签、档位名与 range actions，被禁用的滑杆不改变任何值。
- 校准值经授权修订路径回写样式设置面板契约。
- 所有新增或变更的用户可见文案均提供英文与简体中文字符串，名称完全对齐。
- 普通 Home 交互、编辑模式、收藏编辑、抽屉搜索与启动、其余样式面板设置均无回归。

## 验证要求

除非显式提升，以下为推荐场景：

- 主物理设备旅程：提示出现与几何、Dismiss 与当日抑制跨重启、成为默认、编辑模式隐藏、提示路由。
- 与作者的校准环节：两条滑杆跨三档的候选，记录被接受的值。
- Home 与抽屉文本上的逐字形阴影渲染，包括背景滑杆的抽屉背景不透明度两端。
- 滑杆旅程：两条滑杆的档位变更、跨重启持久、禁用行为、无障碍播报。
- 英文与简体中文资源字符串对齐。
- 普通 Home、编辑模式、抽屉与样式面板旅程回归。

## 相关决策与技术评估

- [Home 行为](../../product/surfaces/home.zh-CN.md)与[Home 呈现](../../product/presentation/home.zh-CN.md) — 提示的归属契约及其呈现值。
- [样式设置面板呈现](../../product/presentation/style-settings-panel.zh-CN.md) — 本迭代校准其延后值的渐缩滑杆契约。
- [产品决策与范围变更治理](../../product-decisions.zh-CN.md) — 校准回写的修订路径。
