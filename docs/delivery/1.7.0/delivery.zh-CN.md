# Launcher4Max 1.7.0 交付

> 语义源：英文。中文对应文档：本文件与 [delivery.md](delivery.md)。
>
> 本记录定义 1.7.0 的计划交付边界。它本身不授权生产实现、提交、推送、打 tag、产物移动、发布或公开发行；这些动作仍需单独授权。

## 版本意图

`1.7.0` 补齐既有产品契约与已交付实现之间的全部已知差异，并使项目达到首次公开发布所需的就绪状态。作者于 2026-09-17 确定版本大纲，分为三个已确认的部分：补齐未实现的契约内容、补齐发布前准备、以及在横屏的产品定义存在之前不将其纳入本版本。

作者于 2026-09-17 确认了范围裁决：用途受限的无障碍服务作为快捷操作迭代的一部分从仅 debug 源集移入 mainline；隐私联系资源中的过期仓库 URL 作为发布前准备修复；本版本的性能预期为作者主设备可跑通已验收的核心旅程，不追求数值性能门槛。

## 交付级别

`Formal release artifact`（正式发布物），定义见[发布治理](../../release.md)。本版本适用该级别的全部门槛，并显式提升一项：正式发布密钥（release keystore）在首个正式产物打包前必须满足安全存储与两份独立加密备份要求。Git tag、GitHub Release 与任何对外发布仍是单独授权动作，不属于版本完成范围。

应用身份为 `com.maxarchm.launcher`。`versionName` 为 `1.7.0`，`versionCode` `8` 按下一个未占用值分配，待标识符提升提交产生时记录。

## 产品引用

- [产品概览](../../../overview.md)
- [产品基础要求](../../requirements/product-foundation.md)
- [快捷操作行为](../../product/features/quick-actions.md)
- [屏幕锁定行为](../../product/features/double-tap-lock.md)
- [隐私与数据处理](../../product/features/privacy.md)
- [Home 行为](../../product/surfaces/home.md)，含默认启动器提示一节
- [Settings 行为](../../product/surfaces/settings.md)，含快捷操作设置子页
- [Home 呈现](../../product/presentation/home.md)
- [Settings 呈现](../../product/presentation/settings.md)
- [抽屉呈现](../../product/presentation/drawer.md)
- [样式设置面板呈现](../../product/presentation/style-settings-panel.md)，含应用大小渐缩滑杆
- [设计基础](../../product/design-foundations.md)
- [导航](../../product/navigation.md)
- [发布治理](../../release.md)
- [验证指南](../../validation.md)
- [ADR-0004：用途受限无障碍服务](../../decisions/0004-purpose-limited-accessibility-service-for-double-tap-lock.zh-CN.md)

## 纳入范围与用户旅程

在本版本完成时，作者可以通过专用的快捷操作设置子页，将 Home 基本信息区的两个快捷操作槽位分别绑定为 No action、Edit mode 或 Screen lock，绑定随手动备份文件保存；在无障碍服务关闭时绑定 Screen lock 会进入已披露的授权流程，且不改变已保存的绑定。无障碍服务以用途受限边界存在于所有构建类型中，因此被绑定的锁屏手势在 release 构建中可用。原独立的"双击锁屏" Settings 项被移除，隐私声明与无障碍披露改用"绑定手势"与"屏幕锁定"表述。

在 Home 上，当 Launcher4Max 不是默认启动器时，作者会在基本信息区与收藏主列表之间看到一张可关闭的默认启动器提示卡；其可见性每次进入前台评估一次，选择操作路由到与 Settings 默认主屏入口相同的系统目的地。两个界面的应用名称、Home 编辑坞提示文案与抽屉搜索框占位提示使用与 Home 基本信息文本相同参数的已接受逐字形阴影，应用大小块呈现两条带校准视觉值的三档渐缩滑杆。

仓库持有发布治理所需的操作决策：APK 保留位置与策略、正式签名密钥的保管与备份流程、权威构建/签名/摘要/安装命令、tag 命名约定与分发渠道，并配有面向公众的 README、安装说明、截图与依赖许可清单。性能预期记录为主设备对核心旅程的验收。

## 排除项

- 横屏模式及竖屏锁定契约之外的任何方向行为，等待作者未来的产品定义。
- 自动备份、定时备份、云备份与任何用户状态上传。
- Git tag、GitHub Release、APK 上传与任何对外发布；这些仍是单独授权动作。
- 任何商店分发，以及随之所需的法律、商店审批或安全专项评审结论；本版本的分发渠道仅为公开 GitHub 仓库。
- 超出迭代 34 所交付清单与处置范围的新第三方 License 呈现。

## 技术方案与风险

迭代 32 与 33 实现已验收的契约，实现细节由开发侧掌握。迭代 34 以一次经授权的产品契约变更（发行边界与性能预期记录）开始，随后进入操作工作；其契约基线按修订规则在作者验收该变更后更新。

主要风险：备份 schema 扩展必须保持旧备份在方向性兼容规则下可导入；以槽位解析替换硬编码的双击/长按手势不得回归编辑模式进入或失败 toast；无障碍服务 mainline 化会改动 release manifest，并需要 ADR-0004 改写为绑定驱动触发；被搁置的抽屉自动化测试簇核心正是滑杆拖拽注入，自定义滑杆的自动化覆盖存在该已知风险；发布门槛（密钥保管、摘要、可追溯性）属于流程工作，无法以代码替代。

## 纳入迭代

| 迭代 | 状态 | 更新日期 | 依据 |
| --- | --- | --- | --- |
| [迭代 32：快捷操作与屏幕锁定](iteration-32-quick-actions-screen-locking.zh-CN.md) | `Completed` | 2026-09-20 | 项目作者于 2026-09-20 在主物理设备上接受了可观察的快捷操作与锁屏结果。 |
| [迭代 33：Home 与抽屉契约补齐](iteration-33-home-drawer-contract-completion.zh-CN.md) | `Completed` | 2026-09-23 | 项目作者在主物理设备上接受了提示、校准后的渐缩滑杆、独立图标/文字档位与隐藏名称位置。 |
| [迭代 34：公开发布就绪](iteration-34-public-release-readiness.zh-CN.md) | `In Progress` | 2026-09-23 | 作者于 2026-09-23 接受仅 GitHub 公开发布的边界与主设备性能预期，该迭代的产品契约阶段由此交付；操作阶段仍未完成。 |

## 迭代证据与结果

### 迭代 32

[契约](iteration-32-quick-actions-screen-locking.zh-CN.md)。于 2026-09-20 `Completed`。实现交付两个可配置 Home 空白槽位及其 No action 默认、绑定的编辑模式与锁屏手势、带槽位弹窗与始终可见服务状态行的快捷操作设置页、方向性兼容规则下绑定纳入备份、移除独立双击锁屏 Settings 项、每一种构建类型 mainline 中的用途受限无障碍服务，以及 ADR-0004 改写为绑定驱动触发。代理本地检查（`git diff --check`、新增 Kotlin 行宽扫描）通过；代理未运行 Gradle 构建或测试（`Not run`）。作者运行的 `./gradlew lint` 在修正 `ModifierParameter` 参数顺序后通过；无需刷新 lint baseline。作者在主物理设备上完成构建、安装与设备检查，并接受契约旅程，报告无明显问题。验收证据为作者报告。剩余建议缺口如实记录、不视为通过：绑定锁屏手势的专用 release APK 验证，与迭代 34 的产物工作协调。契约的产品契约基线为 `da22d3d1c4c3e22105a8d6f96256c1e6ffe626d4`。实现与本证据已作为 `e103a5d07a1f615e9388baebccdc5ba4c9705dae` 合入 `main`，并同步到作者指定的共享 Git 历史。

### 迭代 33

[契约](iteration-33-home-drawer-contract-completion.zh-CN.md)。于 2026-09-23 `Completed`。已交付范围包括 Home 默认启动器提示；Home 与抽屉应用名称、Home 编辑坞提示与抽屉搜索提示的已接受逐字形阴影；校准后的双渐缩滑杆；具备本地与备份兼容的 Home 模块和 Drawer 独立图标/文字档位；同时考虑两档位的 Drawer 几何；以及支持每行一至六项、保留无障碍名称、禁用文字尺寸滑杆并兼容 Home/Drawer 渲染路径的`隐藏`名称位置。作者完成了编译、安装与主物理设备上的纳入旅程检查，报告未发现明显问题；这属于作者报告的验收证据。代理静态检查（`git diff --check`、变更 Kotlin 行宽扫描）通过；代理未运行 Gradle 构建或测试（`Not run`）。已接受的滑杆参数已回写共享样式设置面板契约。交付提交为 `27d7cc37657fb00c1f2d9e74fa11c6fcf3a41d66`、`823d76e1f2866cc38b40e42dedf55799042bf379`、`8a527046dea5ca1dd2fb4eb8ec30d9683aab66a8`、`680b323e1db8ea9d2a7050cc873cfc116c9d1e40` 和 `1ab1d226a7f21732cf1d59a755771ea676372695`。

### 迭代 34

[契约](iteration-34-public-release-readiness.zh-CN.md)。自 2026-09-23 起为 `In Progress`。产品契约阶段已交付：产品概览与产品基础需求记录了仅 GitHub 公开发布的边界与主设备性能预期，隐私联系方式资源在中英文下均指向当前仓库。操作阶段仍未完成，包括权威发布命令与其余发布操作决定、依赖与许可证清单及其第三方 License 呈现决定、公开仓库呈现、正式制品追溯记录，以及主设备上的 release APK 验证。

## 依赖与顺序

迭代 32 必须在迭代 34 的操作工作之前完成：正式发布物必须包含在 release 构建中真实存在的锁屏能力，而只有 mainline 无障碍服务能提供。迭代 34 内部，产品契约阶段先于操作阶段。迭代 33 在代码与契约上均与两者独立、文件重叠低；按作者既有的单线惯例，三个迭代按记录顺序串行执行。此顺序不将工作绑定到分支、终端、贡献者或预测日期。

## 验证

本版本的强制验证环境为作者指定的主物理设备。迭代级验证遵循各迭代契约。版本完成另需满足发布治理的 `Formal release artifact` 门槛：确定的兼容环境、已执行且无未解决纳入路径失败的验证、依赖与许可处置、对发布构建的安全与隐私评审、稳定签名保管及其两份独立加密备份、产物摘要、外部保留与完整的源到产物可追溯性。性能按作者 2026-09-17 裁决以主设备对核心旅程的验收验证；不追求数值门槛。

## 产物与发布要求

验收 APK 必须保留 `com.maxarchm.launcher`，使用已验收的 `1.7.0`/`versionCode 8` 标识，并可追溯到唯一源提交与签名类别。由于该应用身份不同于 `1.0.0` 至 `1.6.0` 的 `com.avenor.launcher`，自这些版本的就地升级不属于受支持路径；由此产生的全新安装行为与受支持的迁移路径由[发布治理](../../release.zh-CN.md)负责。正式签名密钥在首个正式产物打包前必须满足安全存储与两份独立加密备份要求，任何私有签名材料不得进入 Git 或权威文档。打 tag、创建 GitHub Release 与任何上传仍是单独授权动作。

## 已知局限与遗留问题

- 仅有一台物理验证设备；更广的设备、API、OEM 与语言覆盖在执行前保持未知。
- 1.6.0 重构后遗留的被搁置抽屉自动化测试簇（滑杆拖拽注入等）仍未解决；它不是本版本的纳入路径门槛，但限制了该区域的回归信号。
- 第三方 License 呈现在迭代 34 的清单与处置被验收之前保持 `To be decided`。
- 此前版本已接受的文档选择器与 OEM 呈现差异仍是可接受差异。
- 应用身份已从 `com.avenor.launcher` 改为 `com.maxarchm.launcher`；由此产生的全新安装行为与受支持的迁移路径由[发布治理](../../release.zh-CN.md)负责。`1.0.0` 至 `1.6.0` 是未对外发布的作者日常使用基线，不存在本版本会去更新的外部安装。

## 完成标准

- 所有纳入迭代均为 `Completed`，并附单独记录的证据与作者验收。
- 选定范围内，产品契约、实现、测试与交付证据不存在未解决的重大不一致。
- 发布治理的 `Formal release artifact` 门槛全部满足，包括在首个正式产物之前记录的正式签名密钥保管及其两份独立加密备份。
- 最终标识符、分配的 `versionCode`、源提交、签名类别、产物摘要、保留位置、已知缺口与 tag 处置均记录准确。

## 完成结果

尚无最终结果。本版本截至 2026-09-17 处于规划状态。
