# Launcher4Max 项目概览

> 英文语义源：[overview.md](overview.md)。
>
> 本文仅记录已确认的产品意图，并显式标注尚未解决的范围问题；随着产品发现推进，可以修改或替换本文。

## 项目目的

### 问题

Android 主屏幕应帮助用户快速、准确地找到并打开所需应用或查看必要的设备信息。许多启动器不断加入小部件、信息流、广告、推荐和大量自定义项，最终让主屏幕本身变成另一个争夺注意力的入口。

Launcher4Max 首先源于作者的日常需求：用更安静、更直接的方式访问应用和必要的设备信息。更广泛的用户需求及市场差异化目前仍是假设，需要后续验证。

### 产品目标

提供一个克制的 Android 主屏幕，以尽可能少的干扰和认知负担，帮助用户找到并打开当下需要的内容。

## 目标用户

- 首要用户：作者本人，以是否愿意每天使用作为首要产品检验。
- 潜在未来用户：偏好安静、高效、中英双语启动器体验的 Android 用户。
- 明确不服务的用户群：待产品发现后确认，不从当前概念自行推断。

## 产品原则

- **克制且有意图：** 默认界面的每个元素和每次交互，都必须证明其有助于用户找到所需。
- **个人体验优先：** 作者是否愿意每天使用是第一层产品筛选条件；更广泛的适用性另行验证。
- **Home 直接访问：** “克制”约束的是干扰、认知负担和可避免的操作步骤，并不要求 Home 只出现少量应用。收藏始终直接展示在一个由纵向模块和横向织带组成的全宽有序主列表中，不通过分页、文件夹或另一个展示界面访问。
- **重视隐私：** 不收集与核心功能无关的数据。任何未来数据访问都必须在实现前证明必要性、准确告知、遵循最小化原则并完成审查。
- **用户可控：** 不使用暗黑模式、强制参与、误导性权限申请或以延长启动器停留时间为目标的设计。
- **渐进演进：** 长期维护一个连贯演进的产品，不分叉成互不相关的版本。

## 已确认边界

- 产品形态：Android 启动器，目标是作为设备的默认主屏幕应用。
- 面向用户名称：英文与简体中文均为“Launcher4Max”。
- 支持语言：英文和简体中文。
- 发行边界：仅通过 GitHub 仓库公开发布。不包含应用商店提交；创建 GitHub Release、tag 或上传 APK 仍须单独授权。
- 平台基线：普通 Android 手机、仅竖屏，最低支持 Android 12（API 31），主要真实设备验证范围为 Android 16–17（API 36–37）。
- 作者操作适配：交互与布局决策主要针对右手持机并以右手拇指操作，以及左手持机并以右手点控两种方式优化；其他操作姿势在可行时保持可用，但不作为同等优先的产品优化目标。
- 许可证：Apache License 2.0。
- 不做广告和推荐流。

## 版本方向

以下能力层级仅表示非约束性的长期方向，不构成已承诺的产品路线图、发布计划、交付顺序，也不保证任何后续能力一定会实现：

- **V1 — 固定呈现：** 由用户控制、结果确定的应用及选定设备信息展示；不根据用户行为自动调整。
- **V2 — 基础适应：** 可选的行为驱动展示或排序，但必须先满足明确的隐私、用户控制和验证要求。
- **V3 — AI 助手：** 当前产品范围之外的未来候选方向。
- **V4 — Agent 集成：** 当前产品范围之外的未来候选方向。

这些标签描述能力方向，不等同于语义化软件版本，也不将 V2–V4 开发纳入当前范围。

### V1 本地化与权限约束

- V1 完全保持本地：任何需要网络访问、外部服务或在线数据源的能力，未经作者特许不得进入 V1。
- V1 不新增敏感权限——例如通知查看权限——超出已为锁屏接受、用途受限的无障碍授权；任何进一步权限同样需要作者特许。

### V1 到 V2 的变迁

- 变迁不由时间或改动大小触发，而由能力性质触发：第一项被接受的、基于观察行为决定内容呈现或排序的能力。
- 基于行为指来源于观察到的使用情况，例如使用历史、启动频率或时段模式。用户的显式手动选择——手动排序或手动置顶——是用户控制而非行为，仍然属于 V1。
- 任何 V2 能力进入当前契约之前，作者必须显式决定采纳；行为数据及其处理位置必须得到说明；用户控制必须包含关闭与恢复路径；并且必须存在验证计划。V2 采纳会同步修订 V1 原则条款与当前范围声明。
- 对行为数据完全在设备本地收集、存储和处理（本地分析）的 V2 能力，隐私评审与披露负担相应更轻，无需外部服务评审；把行为数据送出设备的能力仍按更重的评审门槛处理。

## 当前产品范围

当前产品契约定义由 Home、Drawer、应用启动和必要 Settings 组成的本地、可离线日用工具闭环。它提供直接收藏管理、受平台边界约束的应用发现，以及可选、用途受限的锁屏能力；该无障碍授权不会成为独立 Launcher 路径的使用条件。Widget、类似文件夹的分组、主题、丰富自定义、联网信息、账号、云同步和服务器开发仍不属于当前产品范围。

[产品基础需求](docs/requirements/product-foundation.zh-CN.md)负责详细的当前范围、平台与交付边界、产品级验收意图、依赖和开放产品问题；适用交互规格负责界面特定行为，其中 [Drawer 契约](docs/product/surfaces/drawer.zh-CN.md)负责应用可见性、资料与 Private Space 行为。本文不建立第二份当前范围清单。

## 可叠加需求

可叠加需求是可以在实际需要时交付，但不定义也不阻塞 V1、V2 及后续能力层级转换的功能。当前示例包括横屏、折叠屏与平板适配、主题与颜色、天气信息和 Widget。可叠加状态本身不要求能力层级变迁：联网依赖的示例（如天气信息）仍可在 V1 内评估，但必须满足 V1 本地化约束所要求的作者特许。文件夹式分组会破坏当前扁平且直接可见的收藏模块组织，并增加二次展示层级，因此不属于可叠加候选。

可叠加需求不会自动进入当前范围。每项仍须通过 Feature 决策检验、显式写入当前产品契约，并满足适用的隐私、安全、验证和维护约束。

## Feature 决策检验

任何 Feature 进入当前范围前，应回答：

1. 它是否让用户更快、更准或更省事地找到所需应用或信息？
2. 默认体验是否依然克制且易于理解？
3. 首要用户是否会经常使用？
4. 用户能否在适用场景中理解、控制、关闭或从中恢复？
5. 它对隐私、无障碍、安全、平台及维护的影响是否可接受？

不能通过上述检验的 Feature，应被拒绝、推迟或重新设计。

## 工程意图

未来工程决策应偏好成熟、可维护的方案，减少依赖，保持清晰边界、安全默认值和可验证性。这些是后续技术评估的约束，不代表已选定任何具体架构、框架、API 或抽象。

项目预期由 AI 承担较大比例的执行工作，并由人工审核。项目作者仍对产品方向、技术决策、质量和最终发布决定负责。

## 治理与文档

- 项目所有权与决策权：项目作者
- Agent 与 Toolkit 路由：[AGENTS.md](AGENTS.md)
- 文档地图与治理规则：[docs/documentation.zh-CN.md](docs/documentation.zh-CN.md)
- 许可证：[LICENSE](LICENSE)

当前开发、验证、发布和产品 Privacy 契约已通过[文档地图](docs/documentation.zh-CN.md)建立。专业系统架构、安全和隐私评估文档要在创建条件具备真实输入后才建立；规划路径不是已完成证据。
